# learn_node.js

The repository for learning node.js.

## Usage

```
$ docker-compose up -d

# get into container
# alpine image doesn't have /bin/bash
# use /bin/sh instead
$ docker-compose exec app /bin/sh
```
